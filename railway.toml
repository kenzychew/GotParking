[build]
builder = "NIXPACKS"
buildCommand = "pip install -r demo/requirements-demo.txt"

[deploy]
startCommand = "uvicorn demo.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
