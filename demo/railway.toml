# Moved: Railway only reads railway.toml/railway.json at the repo root, not
# in subdirectories. The real config now lives at ../railway.toml (repo root).
# This file is no longer read by Railway; kept only as a pointer.
