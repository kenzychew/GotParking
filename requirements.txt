# Human-facing mirror of the Vercel runtime deps. The deployed copy is
# api/requirements.txt (each Python service in vercel.json is rooted at api/
# and installs from there) -- keep the two in lockstep.
lightgbm==4.6.0
numpy==2.5.0
httpx==0.28.1
